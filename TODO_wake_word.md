# 待做：唤醒词集成

状态：未开始（P2 polish 已完成后排入）
创建：2026-05-05

## 背景

`voice_pipeline.py` 当前是「定长录音 → ASR → LLM → TTS」一次性管线，无持续监听。
设计报告中已把"唤醒词"作为对比列点过，但未实现。

## 候选方案

| 方案 | 模型 | 资源占用 | 自训词 | 工作量 | 叙事价值 |
|------|------|---------|--------|--------|----------|
| A. openWakeWord + Silero VAD | ~600KB ONNX | CPU 1-2% | ✅ | ~150 行 | 中 |
| B. 纯 VAD + 关键词后筛 | WebRTC VAD <100KB | 中（每段都跑 ASR）| ✅ | ~80 行 | 低 |
| C. 手势触发（D435+NPU 检测 ✋）| 复用现有 | 0 新增 | n/a | ~50 行 | **高（贴合五引擎叙事）** |

## 推荐路径

竞赛展示：**C（手势）+ A（语音）双模态触发**
- 手势是项目核心叙事，主 demo 用 C
- 语音独立场景用 A，新依赖：`openwakeword`, `silero-vad`

## 切入点

- `voice_pipeline.py::record_until_silence` 替换为持续监听循环
- 新文件 `wake_word.py`：openWakeWord 推理 + 触发回调
- 与 SenseVoice ASR 串联：唤醒 → 启动 ASR 录音窗口（5s） → LLM
- 集成到 `agent.py` 入口：唤醒后进 agent loop

## 阻塞项

- 需先确认板卡 32GB 内存到位（当前仅 8GB，无法跑完整 LLM 验证唤醒后链路）
- 需用户决定唤醒词文本（如"小英特尔"/"嗨边缘"）

## 验收标准

- 5m 连续待机 CPU 占用 < 5%
- 唤醒延迟 < 500ms（从说完关键词到 ASR 开始）
- 误唤醒率 < 1 次/10min（安静环境）
- 与 NPU 视觉栈、LLM 推理并发不互相干扰
